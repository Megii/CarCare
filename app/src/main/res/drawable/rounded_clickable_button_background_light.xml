<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/whiteWith40Alpha">
    <item android:id="@android:id/mask">
        <shape >
            <corners
                android:radius="@dimen/card_view_corner_radius" />
            <solid android:color="@color/whiteWith40Alpha"/>
        </shape>

    </item>
</ripple>